{
  "name": "pivnik-bonus-app",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "description": "Telegram и VK Mini App программы лояльности бара Пивник — единый дизайн, база и безопасная авторизация",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node universal-server.js",
    "dev": "node --watch universal-server.js",
    "check": "node --check server.js && node --check universal-server.js && node --check app.js && node --check vk-platform.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^4.22.2",
    "helmet": "^8.1.0",
    "pg": "^8.22.0",
    "qrcode": "^1.5.4"
  }
}

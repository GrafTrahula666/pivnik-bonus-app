name: Black frosted controls browser smoke

on:
  push:
    branches:
      - ui/black-frosted-controls-smoke-stage6

permissions:
  contents: read

jobs:
  visual-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout Stage 6
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - name: Install exact project dependencies
        run: npm ci

      - name: Materialize exact UI layers
        run: npm run materialize

      - name: Install isolated Playwright runtime
        run: |
          npm install --no-save --package-lock=false playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Run VK + Telegram control-chrome smoke
        run: node scripts/black-frosted-controls-browser-smoke.mjs

      - name: Upload visual evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stage6-black-frosted-controls-smoke
          path: artifacts/black-frosted-controls-smoke
          if-no-files-found: error
          retention-days: 7
